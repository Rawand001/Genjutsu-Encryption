# USED IDE:
vscode

#REQUIRED EXTENSIONS:

-python
-SQLite
-SQLite viewer

#REQUIRED PACKAGES:

-python
-pyqt5 
// pip install pyqt5

-pycryptodome

// pip install pycryptodome


WHAT TO DO:

1-We included the database and also the code of populating it,you can use the database directly,
 and incase anything happened to the database, u can run the topgen.py file to recreate and populate the database again.

2-the main code in included in a file named main.py, the file should be run after the database existence,
 so u can see the complete working program.

